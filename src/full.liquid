<div class="layout layout--col gap">
  <div class="columns" data-overflow-max-cols="2" data-overflow-max-cols-portrait="1" data-overflow-max-cols-lg="3" data-overflow-max-cols-lg-portrait="2">
    <div class="column">
      {% for item in rss.channel.item limit: 10 %}
        {% assign pub_timestamp = item.pubDate | date: "%s" %}
        {% assign now_timestamp = trmnl.system.timestamp_utc %}
        {% assign diff_seconds = now_timestamp | minus: pub_timestamp %}
        {% assign diff_minutes = diff_seconds | divided_by: 60 %}
        {% assign diff_hours = diff_minutes | divided_by: 60 %}
        {% assign diff_days = diff_hours | divided_by: 24 %}
        {% if diff_minutes < 1 %}
          {% assign time_ago = "Just now" %}
        {% elsif diff_minutes < 60 %}
          {% assign time_ago = diff_minutes | append: "m ago" %}
        {% elsif diff_hours < 24 %}
          {% assign time_ago = diff_hours | append: "h ago" %}
        {% else %}
          {% assign time_ago = diff_days | append: "d ago" %}
        {% endif %}
        <div class="item item--emphasis-1 gap--xsmall" data-pixel-perfect="true">
          <div class="meta">
            <span class="index text--small lg:text--base">{{ forloop.index }}</span>
          </div>
          <div class="content">
            <span class="title title--small lg:title--base">{{ item.title }}</span>
            <span class="label label--small label--gray lg:label--base">{{ item.category }} · {{ time_ago }}</span>
            <span class="description description--xsmall lg:description--small">{{ item.description | strip_html }}</span>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>
{{ title_bar }}